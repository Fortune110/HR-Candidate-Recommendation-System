server:
  port: 18080

spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:55433/talent_archive
    username: archive_user
    password: archive_pass
  flyway:
    enabled: true
    locations: classpath:db/migration

openai:
  apiKey: ${OPENAI_API_KEY:}
  model: ${OPENAI_MODEL:gpt-4o-mini}
